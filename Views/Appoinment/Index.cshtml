<div class="row">
    <div class="container">
       
            <div class="form-group row col-2" id="divDoctorList">
                <lable>Select Doctor</lable>
                @Html.DropDownList("doctorId", new SelectList(ViewBag.DocterList, "Id", "Name"),new {@class="form-control select2", @onchange="onDoctorChange()"})
            </div>
        
        <div id="calendar"></div>
    </div>
    
</div>

<partial name="_AddEditAppointment" />